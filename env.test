PORT=port on which app will run
DB_NAME=name of the database
POSTGRES_USER=database username
POSTGRES_PASSWORD=database passowrd
POSTGRES_HOST=database hostname
POSTGRES_PORT=database port, default is 5432
DB_POOL_MAX=max pool size
DB_POOL_MIN=min pool size
DB_POOL_IDLE=pool idle timeout
CUSTOMER_VOUCHER_LIMIT=max voucher to be created at a time to avoid db overload
POSTGRES_DB_USER: User for Postgres db docker image 
POSTGRES_DB_PASSWORD: password for Postgres db docker image 
API_RATE_TTL= ttl for api rate limiter
API_RATE_LIMIT= no of request allowed per ttl
